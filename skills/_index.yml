generated: '2026-08-16'
method: generated
source: >-
  openapi/float-financial-openapi.yml (every operationId referenced was verified present in the spec),
  https://docs.floatfinancial.com/docs/accounting, https://docs.floatfinancial.com/docs/webhooks and
  https://help.floatfinancial.com/hc/en-us/articles/38048585600404-Get-Started-with-Float-s-Public-API
api: Float Public API
base_url: https://api.floatfinancial.com
provider_published_skills: none
provider_published_note: >-
  Float publishes no Agent Skills, no AGENTS.md and no MCP server. These three skills are GENERATED by API
  Evangelist from Float's own OpenAPI and documentation — grounded in real operationIds, real error strings and
  real webhook semantics, with the documented gaps (no sandbox, no rate limits, undeclared error schema, partial
  idempotency coverage) stated rather than papered over.
skill_count: 3
skills:
- name: float-financial-accounting-sync
  file: float-financial-accounting-sync.md
  summary: >-
    Sync card transactions, bills and reimbursements into an external accounting system and mark them exported —
    the flow Float's own accounting guide is built around.
  operations: 24
  writes: true
- name: float-financial-webhook-consumer
  file: float-financial-webhook-consumer.md
  summary: >-
    Register a webhook endpoint, verify the HMAC-SHA256 signature, deduplicate the 10 retries, and hydrate the
    thin event payload into a full card transaction.
  operations: 4
  writes: true
- name: float-financial-spend-reporting
  file: float-financial-spend-reporting.md
  summary: >-
    Read-only reporting: pull card spend, account activity, bills, reimbursements and payments across a date
    window and attribute them to cards, users, teams, vendors and GL codes.
  operations: 17
  writes: false
cross_cutting_rules:
  auth: 'Authorization: Bearer <per-business token> from app.floatfinancial.com Settings > Business Settings > Developers'
  sandbox: none — every call is production
  pagination: page / page_size, loop until page.pages <= page_num, rows in page.items
  filtering: created_at__gte, created_at__lte, order_by on all 21 collection operations
  idempotency: X-Idempotency-Key, REQUIRED on 9 of 22 writes; absent from bulk create and bulk PATCH
  errors: '{"error","message","docs"} in application/json — not RFC 9457 and not declared in the OpenAPI'
  rate_limits: none published, no headers returned — pace yourself
